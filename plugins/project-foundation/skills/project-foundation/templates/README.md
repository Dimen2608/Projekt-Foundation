# <PROJEKTNAME>

> Beantwortet: **Wie starte und benutze ich das Projekt?**
> Nicht: was gebaut wird (`docs/PROJECT.md`) oder wie es aufgebaut ist
> (`docs/ARCHITECTURE.md`).

<Ein bis zwei Sätze, was das Projekt tut.>

## Voraussetzungen

- <Runtime und Version>
- <Weitere Werkzeuge>

## Installation

```bash
<install-command>
```

## Nutzung

```bash
<dev-command>
```

## Entwicklung

```bash
<lint-command>
<typecheck-command>
<test-command>
<build-command>
```

<Wenn Environment-Variablen nötig sind: `cp .env.example .env` und den Verweis auf
`.env.example` hier nennen.>

## Dokumentation

| Frage | Datei |
| --- | --- |
| Was bauen wir? | `docs/PROJECT.md` |
| Wie ist es strukturiert? | `docs/ARCHITECTURE.md` |
| Warum wurde so entschieden? | `docs/decisions/` |
| Was gilt gerade? | `STATUS.md` (falls vorhanden, sonst Zeile streichen) |
